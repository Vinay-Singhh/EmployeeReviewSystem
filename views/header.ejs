<!-- Navbar -->
<nav class="navbar navbar-light" style="background-color: #e3f2fd;">
    <!-- Container wrapper -->
    <div class="container">
        <!-- Navbar brand -->
        <a class="nav-link active text-dark" aria-current="page" href="/">Employee Review System</a>
        <div class="d-flex">

            <% if(locals.user && !locals.user.isAdmin){ %>
                <a class="nav-link text-dark" href="/users/sign-out">Sign Out</a>
                <a class="nav-link text-dark" href="#">
                    <%= user.name %>
                </a>
                <% }else if(locals.user && locals.user.isAdmin){ %>
                    <a class="nav-link text-dark" href="/admin/admin-page">Assign Work</a>
                    <a class="nav-link text-dark" href="/admin/view-employees">Employees</a>
                    <a class="nav-link text-dark" href="/users/sign-up">Register</a>
                    <a class="nav-link text-dark" href="/users/sign-out">Sign Out</a>
                    <% }else{ %>
                        <a class="nav-link text-dark" href="/users/sign-in">Login</a>
                        <a class="btn btn-primary me-3" href="/users/sign-up" role="button">Sign up for free</a>
                        <% } %>

        </div>
    </div>
</nav>